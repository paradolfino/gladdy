<%= form_with(model: ladder, local: true) do |form| %>
  <% if ladder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ladder.errors.count, "error") %> prohibited this ladder from being saved:</h2>

      <ul>
        <% ladder.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.number_field :size %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
